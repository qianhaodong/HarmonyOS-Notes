export interface TabClass {
  title: string
  name: string
  icon: ResourceStr
}

export interface DailyRecommendType {
  img: ResourceStr
  title: string
  type: string
  topColor: ResourceColor
  bottomColor: ResourceColor
}

export interface RecommendListType {
  img: ResourceStr
  title: string
  count: string
}